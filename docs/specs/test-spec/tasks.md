# 実装タスク (tasks.md)

このファイルは MCP ワークフローに従って編集します。

注意: タスクを開始する際は `- [ ]` → `- [-]` に変更し、実装が終わりログを残したら `- [x]` にしてください。

1. [ ] task-1: API ベースの骨組み作成
   - 目的: /api/todos のエンドポイント群 (GET/POST/PUT/DELETE) のルーティングと簡易ハンドラを作る
   - ファイル想定: `src/todos_api.py`（または既存の API ファイルに統合）
   - 成功基準: ハッピーパスのエンドポイントで 200/201/204 が返る

2. [ ] task-2: DB モデルと CRUD 実装
   - 目的: SQLite を使った Todo モデルと永続化の実装
   - ファイル想定: `src/database.py` の拡張または新規 `src/models/todo.py`
   - 成功基準: 作成・一覧・更新・削除が DB に反映される

3. [ ] task-3: 単体テスト追加
   - 目的: pytest を用いた各エンドポイントのテストを追加
   - 成功基準: 主要な API に対するテストが通る

4. [ ] task-4: README の削除手順を追記（完了時に確認）
   - 目的: 作業後に簡単にこのフォルダを取り除けるようにする（既に README に記載済み）
